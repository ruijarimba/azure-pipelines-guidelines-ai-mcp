# Checkout current and shared Helm YAML templates repositories
#
# Careful when editing the checkout or the path as you might break pipelines that depend on this step

steps:
  # checkout folder is $(Pipeline.Workspace)/myself
  - checkout: self
    path: myself
    clean: true

  # checkout folder is $(Pipeline.Workspace)/shared
  - checkout: shared
    path: shared
    clean: true