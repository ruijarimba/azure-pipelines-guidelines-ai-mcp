parameters:
  - name: chartName
    type: string
  - name: namespace
    type: string
    values:
      - default
      - kube-system

variables:
  - name: deploymentMode
    value: upgrade

jobs:
  - job: deployChart
    pool:
      vmImage: ubuntu-latest
    steps:
      - script: helm upgrade $(chartName) ./chart
        displayName: Deploy chart
      - template: charts/deploy-steps.yaml
