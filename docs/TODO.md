# TODO list

This file tracks unresolved work and items to review in a future development session.

## Watch items

- Re-check the companion `guidelines.json` manifest when it changes.
- Implement any newly introduced `ADOG-*` rules using the existing rule workflow.

Review this list at the start of each session. Record completed work in the project history or
the current session handoff instead of keeping completed checklist items here.
